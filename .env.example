# Groq API Key — get free at groq.com/keys
GROQ_API_KEY=your_groq_api_key_here

# Semantic Scholar — NO KEY NEEDED, public API is free without key
# 100 requests per 5 minutes is more than enough for demo usage
# Do NOT add any Semantic Scholar key — just leave this comment here
